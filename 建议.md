# Random Mage / new-pixiv-api 项目建议（2026-02-23）

> 说明：这份文档用于记录对当前项目的功能与工程化建议，方便后续迭代规划。  
> 内容偏“可执行的改进点”，不要求一次性全部实现。

---

## 1) 你现在已经拥有什么

- **公共 API + 管理后台**：`/random` 为核心出图入口，后台负责导入、补全、Token/代理池与任务运维。
- **强筛选能力**：R18/AI/作品类型/作者/分辨率/热度/标签等可组合，且已支持标签组内 `|`（OR）与组间 AND。
- **任务系统**：导入与补全（hydration/backfill）可后台执行，并提供失败查看与重试入口。
- **稳定性基础**：有 `healthz`、运行指标、请求 ID、失败冷却等机制。

---

## 2) 当前最值得优先做的（按收益排序）

### 2.1 SQLite 并发与锁争用（稳定性第一）

如果你仍使用 SQLite（尤其是单文件 DB）：

- **开启 WAL**、合理设置 `busy_timeout`，并在写入高峰（导入/补全/随机请求打点）期间控制写并发。
- 将“频繁小写入”（例如请求统计、last_ok/last_fail 标记）做成 **批量/缓冲写**，降低锁争用与连接池压力。
- 大批量导入时尽量减少 `flush/commit` 次数，或改为分批事务（例如每 500~2000 行一批）。

### 2.2 连接池与超时（避免 5xx）

- 对 SQLite：建议限制连接池规模、避免过多并发连接导致 `QueuePool timeout`。
- 对高并发请求路径：把可选的 DB 写操作降级为“失败可忽略”（例如统计或 last_ok 更新失败不影响出图）。

### 2.3 /docs 与对外可用性（降低使用门槛）

- 把常用示例写清楚：`/random`（图片）、`format=json`（JSON）、`r18`、`orientation`、`adaptive`、标签 AND/OR 规则。
- 新增页面（如 `/status`、`/wtf`）在 `/docs` 中同步更新说明与示例，减少用户探索成本。

### 2.4 镜像上游与网络策略（体验提升）

- 图片上游（如 `i.pximg.net`）在不同网络环境下表现差异大：提供可切换的镜像策略能显著提升可用性。
- 建议记录“镜像命中率/失败率”，用数据指导默认策略（而不是只靠经验）。

---

## 3) 产品层建议（可选，但很有价值）

### 3.1 更“可控”的随机

- 在 `quality` 策略里展示一些可解释信息（抽样数、评分项、被过滤原因）有助于调参。
- 增加一些“更符合直觉”的预设：比如“桌面壁纸”“手机竖屏”“低热度探索”“高热度精选”等。

### 3.2 更好的浏览入口

- 像 `/wtf` 这种“信息流”入口，配合过滤条件可成为主要浏览方式；
- 可加“回到顶部”“暂停/继续”“一键复制当前过滤 URL”等小功能。

---

## 4) 合规与风险提示（务必重视）

- **版权与二次分发边界**：缓存与代理属于敏感区域，建议在文档中明确来源与使用范围。
- **审计与速率限制**：对外公开时，应考虑基础限流、防滥用与日志留存策略。

---

## 5) 一个可执行的迭代路线（参考）

1. 修复高频 5xx：锁争用 / 连接池 / 写入降级  
2. 完善公开文档：/docs + 常用示例 + 语法速记  
3. 增强公开状态：/status + 统计持久化 + 可视化  
4. 体验入口：/wtf 信息流 + 自适应 + 更丝滑加载  
5. 进一步：更强筛选、更可控随机、更多上游策略

