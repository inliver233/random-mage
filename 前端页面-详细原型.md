# 前端页面原型（React + Ant Design）— 逐页/逐按钮/逐接口映射

> 本文是“把你脑子里的管理后台”翻译成可实现的 UI 规格：页面、布局、按钮、校验、以及每个按钮调用哪个后端接口。  
> 目标：比 `pixiv-反代` 那套 AdminJS 更直观、更少页面、更少“迷路”。

---

## 0. 全局 UI 规范

### 0.1 视觉与交互
- 单语言：简体中文（第一版不做 i18n）
- 配色：浅色主题为默认；可选暗色主题（MVP 可不做）
- 布局：`Sider(左菜单) + Header(顶部) + Content`
- Header 固定展示：
  - 当前环境（dev/prod）
  - 版本/commit（来自 `/version`）
  - Worker 心跳（最近一次时间）
  - 一键复制 `request_id`（最近一次请求）

### 0.2 通用组件（必须抽成可复用）
- `PageHeader`：标题 + 描述 + 右侧操作按钮区
- `HealthBadge`：红/黄/绿状态徽章（闭环卡片用）
- `ConfirmDangerModal`：危险操作二次确认（delete/rollback）
- `JsonViewer`：折叠展示 JSON（Import detail / Job detail）
- `ApiErrorAlert`：统一错误提示（含 request_id + 建议下一步）
- `CopyableText`：可复制文本（支持脱敏显示）

### 0.3 URL 与路由

建议管理后台路由：
- `/admin/login`
- `/admin`（Dashboard）
- `/admin/import`（导入）
- `/admin/images`
- `/admin/random`
- `/admin/tags`
- `/admin/authors`
- `/admin/tokens`
- `/admin/proxies`
- `/admin/bindings`
- `/admin/jobs`
- `/admin/settings`
- `/admin/audit`

---

## 1. 登录页（/admin/login）

### 1.1 布局
- 居中 Card（宽 360）
- 表单项：用户名、密码
- Button：`登录`

### 1.2 校验
- 两项必填
- 登录失败：显示 `ApiErrorAlert`（不要提示“账号是否存在”细节）

### 1.3 接口映射
- `POST /admin/api/login`
- 成功：保存 token 到 memory（或 httpOnly cookie），跳转 `/admin`

---

## 2. Dashboard（/admin）

### 2.1 页面分块（从上到下）

#### A) 闭环验收卡片（5 张）
每张卡：标题 + 状态徽章 + 数值 + “下一步”按钮

1) Image 库
- 指标：`images_total`, `images_active`
- 状态：
  - 绿：active>0
  - 红：active=0
- 按钮：`去导入`
- 接口：`GET /admin/api/stats`（若不做 stats 聚合，可先用多个接口拼）

2) Hydration（元信息覆盖）
- 指标：`images_with_tags / images_total`
- 状态：覆盖率阈值（例如 <10% 红）
- 按钮：`创建补全任务`
- 接口：`GET /admin/api/stats`

3) Tokens
- 指标：`enabled_tokens`
- 状态：0 红
- 按钮：`去添加 Token`

4) Proxies（健康）
- 指标：`healthy_proxies`
- 状态：0 红（若 proxy.enabled 且 fail-closed）
- 按钮：`刷新代理` / `探测健康`

5) Random 可用性
- 指标：近 15min random 200%（或 last 100 requests）
- 按钮：`打开 Playground`

#### B) 运行态面板（Descriptions）
- proxy.enabled / fail_closed / route_mode
- easy_proxies：base_url（脱敏）、last_import_at、last_result
- worker：instances、last_heartbeat
- db：wal 是否启用、db 文件大小（可选）

#### C) 快捷操作（Buttons）
- `导入 URL` → `/admin/import`
- `从 easy_proxies 刷新代理`（触发 job）
- `探测代理健康`（触发 job）
- `重新计算绑定`（触发 binding recompute）
- `创建 backfill`（打开 modal）
- `查看 Jobs` → `/admin/jobs`

### 2.2 接口映射（建议合并成一个 stats）
- `GET /admin/api/dashboard`：
  - counts
  - config snapshot
  - easy_proxies snapshot
  - worker snapshot
  - recent error summary

---

## 3. 导入页（/admin/import）

### 3.1 Tabs
- Tab1：粘贴导入
- Tab2：文件上传
- Tab3：导入记录

### 3.2 Tab1：粘贴导入（textarea）

组件：
- `TextArea`（最少 12 行；显示字符数/行数）
- Options（Switch/Select）：
  - `dry_run`（默认 true）
  - `hydrate_on_import`（默认：取 runtime_settings）
  - `source`（manual/file/api）
- Buttons：
  - `预览解析`（dry_run=true）
  - `开始导入`（dry_run=false）

UX 细节：
- 预览时展示：
  - 前 20 条解析成功（illust_id/page/ext）
  - 错误行列表（line/url/code/message）
  - 去重后数量估算
- 开始导入后：
  - Toast：已入队（显示 import_id/job_id）
  - 自动跳转到 Import 详情页

接口映射：
- `POST /admin/api/imports`（json）

### 3.3 Tab2：文件上传

组件：
- Upload（accept `.txt`，可拖拽）
- 同 Tab1 options/buttons

接口映射：
- `POST /admin/api/imports`（multipart）

### 3.4 Tab3：导入记录

Table 列：
- id / created_at / source / total / accepted / success / failed / status（derived）
- 操作：`查看`（进入详情）

接口映射：
- `GET /admin/api/imports?cursor=&limit=`

### 3.5 Import 详情（/admin/import/:id）

显示：
- 进度条：processed/total
- 分布：deduped/success/failed
- 最近错误（前 20）
- 关联 job（若存在）：状态、attempt、last_error

按钮：
- `刷新进度`
- `回滚（disable）`
- `回滚（delete）`（危险）
- `导出错误 URL`（生成可再次导入的文本）

接口映射：
- `GET /admin/api/imports/{id}`
- `POST /admin/api/imports/{id}/rollback`

---

## 4. 图片库（/admin/images）

### 4.1 顶部筛选条

筛选项：
- status（active/disabled/broken）
- r18（0/1/2/unknown）
- orientation（portrait/landscape/square/unknown）
- min_width/min_height/min_pixels
- user_id / illust_id
- tags include / exclude（可输入多值）

按钮：
- `查询`
- `重置`
- `批量入队 hydrate（按当前筛选）`

接口映射：
- `GET /admin/api/images?cursor&limit&filters...`
- `POST /admin/api/hydration-runs`（criteria=当前筛选 or 缺失字段）

### 4.2 Table 列（建议）
- id（可复制）
- illust_id（可复制）
- page_index
- ext
- status（Tag）
- width×height
- x_restrict（Tag：R18/R18G/All/Unknown）
- ai_type（Tag：AI/非AI/未知）
- user（id+name）
- tags_count
- fail_count / last_fail_at
- 操作列（按钮组）

行操作按钮：
- `详情`（Drawer）
- `启用/禁用`
- `入队 hydrate`
- `heal url`（入队）
- `删除（软删）`（危险）

Drawer 详情：
- 基本信息（illust/page/ext）
- URL：
  - proxy stable：`/i/{id}.{ext}`
  - legacy：`/{illust}.{ext}` 与 `/{illust}-{page+1}.{ext}`
  - origin_url（可配置：只对 admin 展示）
- tags（可搜索/复制）
- 错误历史（last_error_code/msg + fail_count）
- 快捷按钮同上

---

## 5. Random Playground（/admin/random）

### 5.1 左侧筛选表单
- format（image/json/redirect）
- attempts（Slider 1..10）
- seed（Input）
- r18 / strict
- orientation
- min_*（InputNumber）
- included/excluded tags（Select mode=tags，支持自由输入）
- user_id / illust_id
- ai_type
- created_from/to（DatePicker）

### 5.2 中间结果区
- image 模式：显示图片（img src 指向 `/random?...`），并显示响应头摘要
- json 模式：JsonViewer（可折叠）
- redirect 模式：显示 Location

### 5.3 右侧工具
- Copy URL
- Copy curl
- “在新窗口打开”

接口映射：
- Public：`GET /random`

---

## 6. Tags（/admin/tags）

列表列：
- name
- translated_name
- count_images
- 操作：详情

详情页：
- 展示关联图片数、最近采样 10 张（缩略图可选）
- 按钮：`从该标签随机一张（跳转到 Playground 预填）`

接口映射：
- `GET /tags`（或 admin 同款）

---

## 7. Authors（/admin/authors）

列表列：
- user_id
- user_name
- count_images
- 操作：详情 / 在 Playground 预填 user_id

接口映射：
- `GET /authors`

---

## 8. Tokens（/admin/tokens）

列表列：
- label
- enabled
- masked（refresh_token_masked）
- weight
- error_count
- backoff_until（相对时间 + 绝对时间）
- last_ok_at/last_fail_at
- 操作

按钮：
- `新增 Token`（Modal）
- `测试刷新`（对单 token）
- `启用/禁用`
- `重置失败退避`

新增 Modal 字段：
- label（可选）
- refresh_token（必填，PasswordInput）
- weight（默认 1.0）
- enabled（默认 true）

接口映射：
- `GET /admin/api/tokens`
- `POST /admin/api/tokens`
- `POST /admin/api/tokens/{id}/test-refresh`
- `POST /admin/api/tokens/{id}/reset-failures`

---

## 9. Proxies（/admin/proxies）

### 9.1 Endpoints 子页（或 Tab）

导入区：
- TextArea（每行一个 URI）
- Select：source（manual）
- Select：conflict_policy（overwrite/skip_non_source/skip_non_manual/skip_non_easy_proxies）
- Button：`导入`

easy_proxies 区：
- base_url（Input）
- password（PasswordInput，可空）
- Button：`从 easy_proxies 导入`
- 显示：last_result

健康区：
- Button：`探测健康（入队）`
- Table：endpoint_id/uri(mask)/enabled/latency/status/blacklisted_until/last_error

接口映射：
- `POST /admin/api/proxies/endpoints/import`
- `POST /admin/api/proxies/easy-proxies/import`
- `POST /admin/api/proxies/probe`

### 9.2 Pools 子页

Pools 列表：
- name/desc/enabled/endpoints_count
- 操作：编辑

Pool 编辑：
- 多选 endpoints（带搜索）
- 每个 endpoint 可设置：enabled/weight
- Button：保存

接口映射：
- `GET/POST/PUT /admin/api/proxy-pools`
- `POST /admin/api/proxy-pools/{id}/endpoints`

---

## 10. Bindings（/admin/bindings）

选择框：
- pool_id（Select）

Table 列：
- token_label/token_id
- primary_proxy（mask 展示 host:port）
- override_proxy（可空）
- override_expires_at（倒计时）
- proxy_health（latency/score）
- 操作按钮

按钮：
- `重新计算 primary`
- `清空全部 override`
- 行按钮：
  - `手工 override`（Modal：选 proxy + TTL）
  - `清空 override`

接口映射：
- `GET /admin/api/bindings?pool_id=`
- `POST /admin/api/bindings/recompute`
- `POST /admin/api/bindings/{id}/override`
- `POST /admin/api/bindings/{id}/clear-override`

---

## 11. Jobs（/admin/jobs）

筛选：
- status（pending/running/failed/dlq）
- type
- ref_type/ref_id（可选）

Table 列：
- id/type/status/attempt/max_attempts/run_after/locked_by/updated_at/last_error

操作：
- `查看详情`（Drawer：payload_json + error + logs）
- `retry`
- `cancel`
- `move_to_dlq`

接口映射：
- `GET /admin/api/jobs`
- `POST /admin/api/jobs/{id}/retry|cancel|move-to-dlq`

---

## 12. Settings（/admin/settings）

表单（按分组）：

Proxy：
- enabled（Switch）
- fail_closed（Switch）
- route_mode（Select：pixiv_only/all/allowlist）
- allowlist_domains（TagsInput）

Random：
- default_attempts（InputNumber）
- default_r18_strict（Switch）
- fail_cooldown_ms（InputNumber）

Security：
- hide_origin_url_in_public_json（Switch）

Rate Limit：
- hydrate_max_in_flight / per_token
- hydrate_min_interval_ms / per_token
- proxy_probe_concurrency

按钮：
- `保存设置`
- `恢复默认`（二次确认）

接口映射：
- `GET /admin/api/settings`
- `PUT /admin/api/settings`

---

## 13. Audit（/admin/audit）

Table 列：
- created_at/actor/action/resource/record_id/request_id
- detail_json（展开）

接口映射：
- `GET /admin/api/audit?cursor&limit&filters`

