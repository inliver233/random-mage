# 安全规范（必须执行）— 敏感信息、访问控制、合规与风控

> 这是硬门槛：只要泄露 refresh_token/代理密码，就等于项目不可用。  
> 本文把“必须怎么做”写清楚，后续实现必须加单测/CI 防回归。

---

## 1. 敏感信息分类

### 1.1 最高敏感（严禁回显/严禁日志）
- Pixiv `refresh_token`
- 代理 URI 中的 `username/password`
- Admin 密码/密钥（SECRET_KEY、encryption key）
- 任何 session cookie / JWT

### 1.2 中敏感（可脱敏展示）
- proxy host:port（可显示）
- token label（可显示）
- request headers 中可能含鉴权（必须过滤）

---

## 2. 存储：加密与 write-only

### 2.1 refresh_token 的存储规则
数据库中：
- 存 `refresh_token_enc`（Fernet/AES-GCM 等）
- 存 `refresh_token_masked`（仅用于 UI）

API 规则：
- 创建 token：允许提交明文
- 读取 token：永远不返回 refresh_token 明文
- 编辑 token：refresh_token 字段永远为空（如需更新必须重新提交）

### 2.2 proxy password 的存储规则
同 refresh_token：
- `password_enc` + UI masked（或不显示密码字段）
- 输出 proxy URI 时必须移除密码（或只显示 `http://user:***@host:port`）

---

## 3. 日志脱敏（必须默认开启）

### 3.1 需要脱敏的字段（全局过滤器）
- 任意包含 `refresh`、`token`、`authorization`、`password` 的键
- 任意符合 proxy uri 模式 `scheme://user:pass@host:port` 的字符串

### 3.2 日志策略
- 生产只打 JSON 结构化日志（stdout）
- 不记录完整请求 body（尤其 admin 导入可能包含隐私）
- 对导入文本：只记录行数/哈希，不记录内容

---

## 4. 管理后台鉴权

最低要求：
- `/admin/api/*` 必须鉴权
- 公网部署时建议二次防护：
  - IP allowlist（仅允许你的 IP）
  - 或 Nginx basic auth

建议方案：
- JWT（短期）+ refresh（可选），或 session cookie

CSRF：
- 若使用 cookie session，必须加 CSRF（至少 Origin/Referer 同源校验）

---

## 5. 公网 API 防滥用（至少做两层）

### 5.1 速率限制（Rate Limit）
对 `/random`：
- IP 维度限流（例如 30 req/min）
- 或 API Key 维度限流（可选）

对 `/i/*`：
- 可稍宽，但要防止恶意扫库（可对不存在的 id 加更严格的限流）

### 5.2 参数限制
- `attempts` clamp `1..10`
- `min_*` 上限 `<=2147483647`
- tags 数量上限（例如 included_tags<=20）
- `seed` 长度上限（例如 <=128）

---

## 6. SSRF 与出站安全

本项目会主动出站访问：
- `oauth.secure.pixiv.net`
- `app-api.pixiv.net`
- `*.pximg.net`
- `easy_proxies base_url`（管理员配置）
- `proxy_probe_url`（配置）

必须限制：
- easy_proxies base_url 仅允许 http/https，且必须是合法 origin（不允许 file:// 等）
- proxy_probe_url 应限制到 allowlist（例如只允许 `http://cp.cloudflare.com/generate_204` 或你指定域名）

若追求“绝不泄露真实 IP”：
- 必须开启 fail-closed
- 并在网络层禁止容器直连外网（仅允许走代理网关）

---

## 7. R18 与合规默认值

默认策略：
- public `/random` 默认 `r18=0` + `r18_strict=1`
- 只有明确传入 `r18=1/2` 才返回 R18/R18G（并建议另加访问控制）

必须提供的合规能力：
- 图片禁用/下架（按 illust_id / tag / author / import 批次）
- 审计日志（谁在什么时候下架了什么）

版权与条款：
- Pixiv 内容受版权与平台条款约束；你对外提供服务应保证使用范围合法合规。

---

## 8. 安全回归测试（CI 门禁）

必须有单测覆盖：
1) `GET /admin/api/tokens` 返回中不含 refresh_token 明文
2) `GET /admin/api/proxies/endpoints` 返回中不含 proxy password 明文
3) 日志脱敏：构造包含 token 的错误对象，确认日志输出被 redacted
4) 导入文本不落日志

