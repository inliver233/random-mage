# Research Notes

`docs/research/` 用于存放**可公开提交**的研究/调研笔记（例如：方案对比、踩坑记录、API 参考链接等）。

约定：
- 个人临时实验/一次性草稿请放在本地 `temp_research/`（已被 `.gitignore` 忽略，不进入仓库）。
- 提交到 `docs/research/` 前请去除敏感信息（API Key、个人路径、项目私密内容等）。
