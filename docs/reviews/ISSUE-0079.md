# ISSUE-0079 — UI Jobs page

This item is a high-level TODO from `TODO-全量Issue拆分.md` and is tracked by a more granular UI issue:
- `ISSUE-0193`: UI page `Jobs` (`/admin/jobs`)

Verification:
- `pwsh -NoProfile -File scripts/test_frontend.ps1` (SKIP: `frontend/package.json` not found)

Notes:
- This ISSUE is redundant; progress is tracked in the superseding issue above.
