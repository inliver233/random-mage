# ANE-0080 — 现存并发实现清单（写死）

## 结论
现存并发实现清单已在 `docs/architecture/tasks.md` 写死，并对 3 个主要并发路径分别做了可追溯定位记录，便于后续替换/收敛到 TaskManager/IndexScheduler。

## 证据
- `docs/architecture/tasks.md`：包含“现存并发实现清单（待替换参考）”
- 细分定位（落盘）：
  - `docs/reviews/ANE-0081.md`：core 自动索引线程（`ProjectManager._trigger_auto_indexing_async`）
  - `docs/reviews/ANE-0082.md`：AIWorkerThread(QThread)（completion/AI 请求线程）
  - `docs/reviews/ANE-0083.md`：MainWindow 保存后索引线程池（QTimer + QThreadPool/QRunnable）

