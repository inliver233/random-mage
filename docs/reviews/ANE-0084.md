# ANE-0084 — Task key 规范与示例（写死）

## 结论
Task key 的规范与示例已在 `docs/architecture/tasks.md` 中写死，包含 key 的命名约束（稳定/可分组/可读）与固定示例列表，可作为后续 TaskManager/IndexScheduler 接入的唯一来源。

## 证据
- `docs/architecture/tasks.md`：
  - “任务 Key 规范（必须遵守）”章节：定义 key 的格式与约束（`<domain>/<action>/<scope>`，动态参数使用 `:`）
  - “写死示例”列表：包含 RAG/Codex/AI/Outline/Import/Export 等关键任务 key

