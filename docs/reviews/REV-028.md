# REV-028 - [quality][naming] 外部项目标识清理扫描 mumu Amily

## 背景
本条目用于确认仓库内不存在外部项目标识（如 `mumu` / `Amily` / `amily`）的残留引用，避免误导与命名污染。

## 检查项
- 代码与文档中是否出现外部项目名/标识
- 若出现：判断是否需要清理（重命名/替换/移除）并给出计划

## 证据（命令与关键输出摘要）
- 命令：`rg -n '\\b(?:mumu|Amily|amily)\\b' backend frontend test docs -S`
- 结果：无命中（rg exit code=1 表示未匹配到任何结果）

## 结论
本次扫描未发现 `mumu` / `Amily` / `amily` 的代码或文档残留，无需清理与 rename 计划。

