# ANE-0064 — BackupService 最小 API

## 结论
已提供最小 API：创建备份集、恢复备份集、列出备份集，且接口保持单入口、参数明确，便于后续 GUI/CLI 调用。

## 证据
- `create_backup_set(project_path, db_paths, reason)`
- `restore_backup_set(project_path, backup_dir)`
- `list_backup_sets(project_path)`
