# ANE-0076 — 第一批接入点（写死）

## 结论
Phase 1 的“第一批接入点（高频/高风险）”已在 `docs/architecture/tasks.md` 写死，用于指导 TaskManager/IndexScheduler 的接入顺序与验收目标。

## 证据
- `docs/architecture/tasks.md` → “第一批接入点（写死）”章节列出 3 项：
  - RAG 索引（全量/增量）
  - Codex 引用检测（节流）
  - AI completion 请求（可取消/超时）

