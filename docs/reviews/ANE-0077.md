# ANE-0077 — 第一批接入点：RAG 索引（全量/增量）

## 结论
“RAG 索引（全量/增量）”已被写死为 Phase 1 第一批接入点之一，并已具备集中调度入口（IndexScheduler）用于后续接入 TaskManager 的去重/节流/取消语义。

## 证据
- `docs/architecture/tasks.md`：第一批接入点包含 “RAG 索引（全量/增量）”
- `src/gui/services/index_scheduler.py`：提供 `schedule_full_scan()` / `schedule_document_index()` 等调度入口（Phase 1 先落地但不移除旧路径）

