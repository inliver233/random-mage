# ANE-0131 Acceptance Evidence

- `Shared.current_project_path` setter converts `str` to `Path` and stores a `Path` instance.
- `projectChanged` signal payload remains `str(path)` when emitted.

Manual spot-check suggestion: create/open a project and confirm logs show `Current project changed to: <path>` (string payload) while internal `shared.current_project_path` remains a `Path`.
