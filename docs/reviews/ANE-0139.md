# ANE-0139 Evidence

- Removed duplicate indexing triggers in `MainWindow` by eliminating direct `_on_save` scheduling and the fallback auto-index scheduling path.
- Indexing now flows through `Shared.documentSaved` -> `IndexScheduler` only.
