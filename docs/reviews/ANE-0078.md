# ANE-0078 — 第一批接入点：Codex 引用检测（节流）

## 结论
“Codex 引用检测（节流）”已被写死为 Phase 1 第一批接入点之一，并在 `docs/architecture/tasks.md` 中定义了对应任务 key 与替换顺序约束，作为后续接入 TaskManager 的执行合同。

## 证据
- `docs/architecture/tasks.md`：
  - 第一批接入点包含 “Codex 引用检测（节流）”
  - key 示例包含：`codex/ref_detect/doc:{document_id}`、`codex/ref_detect/full_scan`

