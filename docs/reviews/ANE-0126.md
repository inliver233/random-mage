# ANE-0126 Evidence

- `ProjectManager.create_project` and `ProjectManager.open_project` no longer emit `self._shared.projectChanged` directly.
- `projectChanged` is emitted only inside `Shared.current_project_path` setter in `src/core/shared.py`.
