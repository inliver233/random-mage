# ANE-0048 — 保存路径不触发备份的原因说明

## 结论
`DatabaseManager.save_project_data()` 采用 `DELETE FROM documents` 后全量重插，属于高频写入路径；若每次保存都触发备份会导致备份膨胀与性能下降，因此备份只限定在危险操作（迁移/导入/删除/清空等）中显式触发。

## 证据
- `src/core/database_manager.py` 的 `save_project_data` 逻辑包含全量删除与批量写入
