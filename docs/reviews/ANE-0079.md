# ANE-0079 — 第一批接入点：AI completion 请求（可取消/超时）

## 结论
“AI completion 请求（可取消/超时）”已被写死为 Phase 1 第一批接入点之一，并在 `docs/architecture/tasks.md` 中定义了对应任务 key 与替换顺序约束；现有实现以 `AIWorkerThread(QThread)` 为主，后续需纳入 TaskManager 统一取消/错误上报。

## 证据
- `docs/architecture/tasks.md`：
  - 第一批接入点包含 “AI completion 请求（可取消/超时）”
  - key 示例包含：`ai/complete/request:{editor_id}`、`ai/complete/stream:{editor_id}`
- `src/core/ai_qt_client.py`：
  - `AIWorkerThread` 提供 `cancel_request()`（协作式取消标记）并通过 signals 上报错误/完成

