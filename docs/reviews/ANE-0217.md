# ANE-0217 — 保留 completion_widget（建议重命名）

## 结论
`src/gui/editor/completion_widget.py` 作为 popup completion 实现保留，建议后续重命名为 `completion_popup.py` 以避免与 AI setting 同名。

## 证据
- 文件存在：`src/gui/editor/completion_widget.py`

