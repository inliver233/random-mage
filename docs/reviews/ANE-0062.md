# ANE-0062 — BackupService v1 接口面

## 结论
已提供硬编码的 BackupService v1 接口面，覆盖创建备份集、恢复备份集与列出备份集三类核心操作，作为重构期统一入口。

## 证据
- `src/core/backup_service.py` 暴露 `create_backup_set` / `restore_backup_set` / `list_backup_sets`
