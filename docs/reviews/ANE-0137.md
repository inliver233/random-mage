# ANE-0137 Evidence

- Full-scan indexing uses TaskManager key `rag/index/full_scan` with `cancel_previous=True` and `coalesce=True` in `IndexScheduler.schedule_full_scan`, ensuring only one full scan runs per project change and new triggers cancel previous runs.
