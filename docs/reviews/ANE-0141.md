# ANE-0141 Evidence

- Indexing is triggered only via `Shared.documentSaved` handled by `IndexScheduler`.
- `MainWindow._on_save` no longer schedules indexing directly, and the fallback auto-index path was removed.
