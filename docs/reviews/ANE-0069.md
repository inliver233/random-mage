# ANE-0069 — 危险操作备份失败即阻止

## 结论
迁移/导入/clear_all 等危险操作在执行前均要求备份成功；若备份失败则直接中止流程并提示用户。

## 证据
- 迁移前备份失败：抛出异常阻止迁移（`src/core/database_manager.py`）
- 导入前备份失败：提示并取消导入（`src/gui/dialogs/import_dialog.py`）
- clear_all 前备份失败：抛错终止清空（`src/core/sqlite_vector_store.py`）
