# ANE-0179 — vectors.db 全局路径现状

## 结论
当前 `vectors.db` 在启动时硬编码为全局路径 `~/.ai-novel-editor/vector_store/vectors.db`，符合“现状写死”记录。

## 证据
- `src/main.py`：使用 `os.path.expanduser("~/.ai-novel-editor/vector_store")` 并构造 `db_path = os.path.join(db_dir, "vectors.db")`

