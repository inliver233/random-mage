# ANE-0138 Evidence

- `IndexScheduler.schedule_document_index` uses key `rag/index/doc:{document_id}` with `cancel_previous=True` and `coalesce=True`, ensuring rapid saves coalesce to the latest per document.
