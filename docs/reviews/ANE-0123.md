# ANE-0123 — 调用点记录：create_project / open_project

## 结论
`_trigger_auto_indexing_async()` 的调用点位于 `ProjectManager.create_project()` 与 `ProjectManager.open_project()` 中（原计划指出的 177/214 行附近）。这些调用已在 ANE-0122 中移除，但调用点位置已在此记录用于追溯。

## 证据
- `src/core/project.py`：`create_project()` 与 `open_project()` 成功路径中曾调用 `_trigger_auto_indexing_async()`
- 相关移除记录：ANE-0122 提交移除了上述调用点

