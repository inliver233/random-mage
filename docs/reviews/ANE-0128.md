# ANE-0128 — open_project 内手动 emit 记录

## 结论
`open_project()` 中曾存在对 `self._shared.projectChanged.emit(...)` 的手动触发（原计划标注 210-212 行附近），现已移除以确保项目变化信号仅由 `Shared.current_project_path` setter 触发。

## 证据
- `src/core/project.py`：open_project 成功路径仅设置 `self._shared.current_project_path`，不再手动 emit
- 相关移除记录：ANE-0125 提交移除手动 emit

