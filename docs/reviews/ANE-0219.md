# ANE-0219 — completion_widget 命名建议

## 结论
`src/gui/ai/completion_widget.py` 当前承载 AI 补全建议 UI，但在 AI 配置侧更像“设置/配置面板”。建议后续统一重命名为 `completion_settings_widget.py`，并同步类名与引用位置，避免语义误导与命名歧义。

## 证据
- 文件存在：`src/gui/ai/completion_widget.py`
- 主要类：`CompletionWidget`（AI 补全建议主界面）
