# ISSUE-0089 — 手工导入多行 proxy URI（冲突策略）

This is a high-level TODO from `TODO-全量Issue拆分.md` and is covered by:
- `ISSUE-0159`: `POST /admin/api/proxies/endpoints/import` (supports `conflict_policy`)
- `ISSUE-0191`: UI page `Proxies` (`/admin/proxies`) (manual import UI)

Verification:
- `pwsh -NoProfile -File scripts/test_backend.ps1` (PASS)

Notes:
- This ISSUE is redundant; progress is tracked in the superseding issues above.

