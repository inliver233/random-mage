## ANE-0118 默认实现：网络 embedder

当前约定：

- 当未传入 `embedder` 时，`RAGService` 继续使用现有网络嵌入逻辑（`create_embedding_async` 内部调用远端 API）。
- 只有显式注入 `embedder` 时才走本地 deterministic 路径。

这保证了生产默认行为不变，测试可用 FakeEmbedder 覆盖。
