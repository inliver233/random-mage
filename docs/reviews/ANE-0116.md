# ANE-0116 — embedding 生成位置（RAGService.create_embedding_async）

## 结论
当前 embedding 生成逻辑位于 `RAGService.create_embedding_async()`，属于后续 Golden Master/可注入 embedder 的明确接入点。

## 证据
- `src/core/rag_service.py`：`async def create_embedding_async(self, text: str, ...)`（约 266 行起），内部调用 `self.base_url/embeddings` 并处理重试/超时
- 该方法被多处调用（例如批量嵌入与查询流程中），符合“现状 embedding 生成位置”的记录要求

