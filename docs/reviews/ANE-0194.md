# ANE-0194 — IntelligentContextBuilder 缓存 shared.rag_service

## 结论
`IntelligentContextBuilder.__init__()` 会从 `shared` 读取并缓存 `rag_service`（以及 `codex_manager`），符合“现状写死”的记录要求。

## 证据
- `src/gui/ai/enhanced_ai_manager.py`：`__init__` 中 `self.rag_service = getattr(shared, 'rag_service', None)`

